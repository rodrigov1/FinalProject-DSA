#include <string>
#include "Cola.h"

using namespace std;

class Router {
    private:
    byte id;
    byte terminal;

    public:
    Router(){;};
    //~Router();
    void setId();
    byte getId();
    
};
